import Box from "@mui/material/Box";
export default function SettingPage() {
  return (
    <Box
      sx={{
        display: "flex",
        minHeight: "100vh",
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      Setting
    </Box>
  );
}
